// Including lint command so that function doesn't have to be used
export default class Filter {
    constructor(family, majorEvents) { // eslint-disable-line no-unused-vars
        this.family = family;
        this.majorEvents = majorEvents;
    }
}