// Including lint command so that function doesn't have to be used
export default class Filter {
    constructor(isMajorEvent) { // eslint-disable-line no-unused-vars
        this.isMajorEvent = isMajorEvent;
    }
}